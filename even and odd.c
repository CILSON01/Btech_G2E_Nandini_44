#include <stdio.h>

int main() {
    int num1;

    printf("Enter the number: ");
    scanf("%d", &num1);

    if (num1 /2) {
        printf("the number entered is even.\n");
    } else {
        printf("The numer entered is odd .\n");
    }

    return 0;
}